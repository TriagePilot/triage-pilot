services:
  web:
    image: ${TRIAGEPILOT_IMAGE:-ghcr.io/triagepilot/triage-pilot:1.1.0}
    build: !reset null

  worker:
    image: ${TRIAGEPILOT_IMAGE:-ghcr.io/triagepilot/triage-pilot:1.1.0}
    build: !reset null
